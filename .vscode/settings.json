// Workspace settings
{
    // The following will hide the js and map files in the editor
    "files.exclude": {
        // "**/*.js": true,
        // "**/*.map": true
        ".bundle": true,
        ".jekyll-cache": true,
        ".jekyll-metadata": true,
        "vendor": true
    }
}
